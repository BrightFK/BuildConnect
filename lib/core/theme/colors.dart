import 'package:flutter/material.dart';

class AppColors {
  // Background & Surface
  static const background = Color(0xFF0F0F0F); // Very dark grey
  static const surface = Color(0xFF1E1E1E); // Slightly lighter for cards
  static const card = Color(0xFF2A2A2A); // Card background (light grey)

  // Primary & Accent
  static const primary = Color(0xFF6C63FF); // Vibrant purple‑blue
  static const secondary = Color(0xFF00D4FF); // Cyan for highlights
  static const accent = Color(0xFFFF6588); // Rose for contrast

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0B0);
  static const textHint = Color(0xFF707070);

  // Status
  static const success = Color(0xFF00C853);
  static const error = Color(0xFFFF1744);
  static const warning = Color(0xFFFFAB00);

  // Shadows
  static const shadow = Color(0x40000000); // 25% black for shadows
}
